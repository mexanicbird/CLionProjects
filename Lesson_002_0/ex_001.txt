// Автор: Теплых Максим, домашнее задание для урока №2 "Основы языка с"

#include <stdio.h>
//#include <stdlib.h>


int main(int argc, char *argv[]) {
// Задание 1:

    printf("Задание 1:\n");
    int a;
    printf("Введите, пожалуйста, целое число: ");
    scanf("%i", &a);
    if ((a >= 0 )&&(a <= 100))
    {
        printf("Число %d входит в диапазон от 0 до 100.\n", a );
    }
    else if (a > 100)
    {
        printf("Число %d больше 100.\n", a);
    }
    else if (a < 0)
    {
        printf("Число %d меньше 0.\n", a);
    }

     // Задание 2:
     /*Т.к. чисел десять и среднее арифметическое буде вычислять деля всю сумму
     на десять, то после запятой у нас будет только одна цифра, а остальные нули.
     Поэтому было принято решение выводить только два знака после запятой!*/

     printf("\nЗадание 2:\n");
    int b = 0, c ;
    double res,d = 0;
    printf("Введите, пожалуйста, десять произвольных целых чисел: \n");
    while(b < 10)
    {
        //	rewind(stdin);
        scanf("%d", &c);
        d += c;
        b++;
    }
    res = d / 10;
    printf("Среднее арифметическое введенных вами чисел равняется: %.2f\n", res);

//Зaдание 3:

    printf("\nЗадание 3:\n");
    int e, f;
    do
    {
        printf("Введите, пожалуйста, целое положительное число не больше 1000: ");
        scanf("%d", &e);
        if ((e >= 0)&&(e <= 1000))
        {
            printf("Все четные числа от нуля до %d: ", e);
            for(f = 0; f <= e; f++)
            {
                printf("%d, ", f);
                f++;
            }
        }
        else
        {
            printf("Вы ввели некорректные данные! Попробуйте снова!\n");
        }
    }
    while (( e >= 0)^(e <= 1000));
    printf("\n");

// Задание 4:
/*В задании сказано, что вывести треугольник спомощью циклов и знака '^',
мною было понято что "^" это именно символ из которых должен состоять
треугольник а не бинарный оператор "XOR"!*/

    printf("\nЗадание 4:\n");
    int i, k, l, m, n;
    do
    {
        printf("Введите, пожалуйста, высоту треугольника: ");
        scanf("%d", &n);
        if (n>0)
        {
            n *= 2;
            for(k=0;k<=n;++k)
            {
                for(i=0; i<n; i++)
                {
                    printf(" ");
                }
                for(l=0; l<k; l++)
                {
                    printf("^");
                }
                for(m=1; m<k;m++)
                {
                    printf("^");
                }
                printf("\n");
                --n;
            }
        }
        else
        {
            printf("Ошибка! Введите значение заново!\n");
        }
    }
    while(n<0);
    printf("Thanks!");




    return 0;
}